{
  "name": "portfolio-landing-system",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c red,cyan \"npm --workspace backend run dev\" \"npm --workspace frontend run dev\"",
    "build": "npm --workspace backend run build && npm --workspace frontend run build",
    "capture:sites": "node scripts/capture-sites.js",
    "typecheck": "npm --workspaces run typecheck",
    "start": "npm --workspace backend run start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "playwright": "^1.60.0"
  }
}
